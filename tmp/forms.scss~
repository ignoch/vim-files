/* Icons by Silk Icons: http://www.famfamfam.com/lab/icons/silk/ */
@mixin button-global {
  display:block;
  float:left;
  margin:0 7px 0 0;
  background-color:#f5f5f5;
  border:1px solid #dedede;
  border-top:1px solid #eee;
  border-left:1px solid #eee;

  font-family:"Lucida Grande", Tahoma, Arial, Verdana, sans-serif;
  font-size:100%;
  line-height:130%;
  text-decoration:none;
  font-weight:bold;
  color:#565656;
  cursor:pointer;
  padding:5px 10px 6px 7px; /* Links */  
}

@mixin button-image {
  margin:0 3px -3px 0 !important;
  padding:0;
  border:none;
  width:16px;
  height:16px;
}

@mixin text-input-format {
  padding: 5px 0 5px 5px;
  width:208px;
  border: 1px solid #ccc; 
}

*:first-child+html .buttons {
  a,img {
    padding:4px 10px 3px 7px; /* IE7 */
  }
}

input[type="text"], input[type="password"] {
  @include text-input-format;
}
span.required {
  float:left;
  display: inline-block;
  color: #c00;
  width:7px;
}
label {
  display:inline-block;
  width: 90%;

}

input.small {
  width:25px;
}

input.medium {
  width: 50px;
}


.buttons {
  a {
    @include button-global;
    img {
      @include button-image;
    }
  }
  button {
    @include button-global;
    width:auto;
    overflow:visible;
    padding:4px 10px 3px 7px; /* IE6 */

    img{
      @include button-image;
    }

  }
  button[type] {
    padding:5px 10px 5px 7px; /* Firefox */
    line-height:17px; /* Safari */
  }
}
