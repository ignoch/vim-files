$background-color: #f2f3e8;
$blue:  #0160b2;    
$green: #97c42d;
$shadow-color: #ccc;
$top-shadow:   3px;
$left-shadow:  2px;
$depth-shadow: 7px; 
$comment-font-color: #999;
$hover-background-color: #f9f9f9;

@mixin p-sub-styles {
  background:$green none;
  color: #fff;
}
@mixin shadow {
  -moz-box-shadow: $left-shadow $top-shadow $depth-shadow $shadow-color;
  -webkit-box-shadow: $left-shadow $top-shadow $depth-shadow $shadow-color;
  box-shadow: $left-shadow $top-shadow $depth-shadow $shadow-color;
} 
@mixin reset-li {
  display: inline;
}

@mixin select-style {
  width:200px;
  height:250px;
}

body {
}

a {
  color: $blue;
  text-decoration:none;
}

li {
  list-style-type:none !important;
}

h1 {
	margin: 0.6em 0;
}

select {
  width: 220px;  
}

#wrapper {
  padding:5px;
}

#logo {
  float:left;
	height: 46px;
}

/* Login Structure */
.login {
  #body {
    @include shadow;
    h2 {
      position: relative;
      background:$blue none;
      color:#fff;
      text-align:right;
      padding:15px 10px;
      img {
        position: absolute;
        top: 2px;
        left: 5px;
      }
    }
  }
  p.sub {
    position: relative;
    float:left;
    padding:10px 30px;
    margin-bottom:10px;
    font-size:20px;
    width:300px;
    @include p-sub-styles;
    span {
      background: transparent url('images/cuadro.jpg') no-repeat 1px -1px scroll;
      position: absolute;
      left:-35px;
      top:0;
      padding:22.5px 20px;
      height: 14px;
    }
  }
  p {
    padding:0 1em 10px;
    margin:0;
  }
}

/* Top Navigation */
#navigation {
  background:$blue none;
  margin-left: 146px;
  display: block;
}

.dropdown {
  float:left;
  /*width:600px;*/
  margin:1px 0 0 3px;
  background: $blue none;
  li {
    border-bottom: 1px solid #567dad;
    display: inline;
    float:left;
    position:relative;
		
    a {
      float:left;
      padding:14px 15px;
      text-decoration: none;
      font-size:13px;
      text-align:center;
      font-weight:bold;
      background:$blue none;
      color:#fff;

      &:hover {
        color:$green;
      }
    }
    ul {
      position:absolute;
      margin:0;
      display:none;
      top:45px;
      left:0;
      li {
        a {
          font-size:13px;
          width:100px;
          display: block !important;
          text-align:left !important;
          padding: 5px 20px;
          width:250px;
        }
        ul {
          top:0px;
          left:290px;
        }
      }
    }
    &.selected {
      a {
        color:$green;
      }
      ul li{
        a{
          color:#fff;
          &:hover {
            color:$green;
          }
        }
      }
    }
  }
}

/* Dashboard Structure */
.dashboard {
  
  #main {
    margin: 5px;
    padding: 5px 20px 15px;
    border:$shadow-color solid 1px;
    @include shadow;
  }
  
  hr {
		border: 0px;
    border-bottom:$blue 1px dotted;
  }
  
  ul.status {
    margin:0;
    li {
      @include reset-li;
      a {
        text-decoration: none;
        font-size:10px;
        &:hover {
          text-decoration:underline;
          color: $blue;
        }
      }
    }
  }
  table {
    width:100%;
    td {
      border-bottom:$shadow-color 1px solid;
      &.small {
	width:20px;
      }
    }
  }

  #logout {
    color:#fff;
    float:right;
    font-size: 0.8em;
    /*line-height: 46px;*/
    text-align:right;
    padding: 0px 15px;
    vertical-align: middle;
    a {
      color:#fff;
      text-decoration: underline;
    }
    p {
      margin:0; padding:5px 10px;
      strong {
        font-size:1em;
      }
    }
  }
}

/* Sidebar Structure */
.sidebar {
  .container {
    margin:5px 10px;
    border:$shadow-color solid 1px;
    @include shadow;
  }
  #search{
    .title {
      padding:3px 10px;
      background: transparent url(../images/bg_title_search.png) repeat-x 0 0 scroll;
    }
    input[type="text"] {
      width:190px;
      margin:5px 10px;
    }
    .campaign-details {
      .unit {
        padding: 3px 5px;
      }
    }
  }
}

/* Form Structure */

.field {
	padding: 3px 0px;
}

.actions {
	padding: 3px 10px;
}

.container {
	
	h1 {
          border-bottom: $blue 2px solid;
	}
	
	h2, h3 {
          margin: 0px 0px 2px;
	}
	
	.comments {
	  color: $comment-font-color;
	  font-size: 0.85em;
	  vertical-align: baseline;
          line-height: 16px;
	}
	
	span.information {
          margin-right: 5px;
          a {
            color: #999 !important;
	    font-size: 0.85em;
	    vertical-align: baseline;
            line-height: 16px;
          }
	}
	
	img.icon {
		height: 13px;
		margin-bottom: 2px;
		vertical-align: bottom;
	}
	
	.bottom-bar {
		line-height: 30px;
	}
	
	.buttons {		
       	  &.delete {
	    a {
	      float: right;				
	    }
	  }
        }
}

/* Campaign Structures */
.fields {
  margin:10px 0;
  padding:5px;
  border:#ddd 1px solid;
  #image {
    width:280px;
    .image-preview {
      float:left;
      width:100px;
      min-height:100px;
    }
    .image-data {
      margin:2px 4px;
      float:left;
    }
    .image-add {
      width:100%;
      #campaign_item_image {
        width:100%;
      }
    }
  }
  table.days {
    td,th {
      border:$shadow-color 1px solid;
    }
  }
  .week {
    position: relative;
    a {
      position:absolute;
      bottom:0;
    }
  }
  .date-start, .date-end {
    width:120px; 
  }
  #campaign_item_campaign_id {
    width:220px;
  }
}
.popup {
    display:none;
    width:800px;
    padding:5px;
    background:#fff none; 
    .fields {
      border:none;
    }
    select {
      width:120px;
    }
}
.campaign_images, #results {
  padding:10px 0;
  .image {
    width:100px;
    margin:5px;
  }
  .details {
    font-size:9px;
    margin:0 9px 0 4px;
    width:100px;
    .week table {
      border:#ddd 1px solid;
      td {
        text-align:center;
        border:none;
      }
    }
  }
}

.week table {
  /*border:#ddd 1px solid;*/
    td {
      text-align:center;
      border:none;
    }
}



/* Groups Structure */
#group-equips {
  select {
    @include select-style; 
 } 
}

/* Equip Structures */
#behavior {
  textarea {
    width:99%;
    height:300px;
  }
  select {
    width:220px;
  }
  .column {
    height: 50px;
  }
}
.serial-container {
  position:relative;
}
#serial {
  display:none;
  position:absolute;
  top:-279px;
  left:224px;
  padding:20px;
  float:left;
  width:280px;
  background:#ccc none;
  select{
    width:220px;
  }
  .gen {
    margin:5px 0;
  }
}
#equips {
 select{
   width: 220px;
 }
}
/* Client Structure */
#clients {
  .client {
    padding: 5px 10px;
    border-bottom: #ddd 1px solid;

		&:hover {
			background-color: $hover-background-color;
		}

    .column {
      .container {

      }
    }
  }
}

.client-state {
	height: 30px;
	vertical-align: middle;
}

/* Users Structure */
#users {
  .columns {
    padding:10px 0;
    border-bottom:#ddd 1px solid;
  }

}

/* Report Structure */
#report {
  select {
    width:220px;
  }
  .column {
    height:50px;
  }
}


/* List Control Structure */
#list-control {
  .container {
    margin:5px;
    select {
      @include select-style;
    }
  }
  a {
    float:left;
    cursor: pointer;
    padding:26px 31px;
    margin:15px 65px;
    background: transparent url(../images/arrows.jpg) no-repeat 0 0 scroll;
    &.right-arrow {
      background-position: -64px 0;
    }
  }
  .field {
    p {
      margin:0;
      float:left;
      font-size:11px;
    }
  }
  select {
    width:220px;
    heigth:180px;
  }
}

/* Report Structure */
#monitor_table, .monitor_table {
  tr th {
    background: #ddd none;
    border:#ccc 1px solid;
    text-align:center;
    font-weight:bold;
    padding:5px 0;
  }
}

.blue-table {
  background: none repeat scroll 0 0 #FFFFFF;
  border-collapse: collapse;
  font-family: "Lucida Sans Unicode","Lucida Grande",Sans-Serif;
  font-size: 12px;
  margin: 20px;
  text-align: left;
  width: 600px !important;
  th {
    border-bottom: 2px solid #6678B1;
    color: #003399;
    font-size: 14px;
    font-weight: normal;
    padding: 10px 8px;
  }
  tr:hover td {
    color: #000099;
  }
  td {
    color: #666699;
    padding: 9px 8px 0;
  }
}

#treelist {
  .container {
    display:block;
    min-height: 20px;
    padding:2px 5px; 
    margin:3px;  
  }
  .group, .equip {
    cursor: move;
    border:#ff0021 1px solid;
    margin:5px;
  }
  .equip {
    border-color:#00c127;
  }
  .group {
    width:233px;
  }
}


/* Common Classes */
.clear {
  clear:both;
}
.left {
  float:left;
}
.error, .notice, .alert {
 border: 2px solid #DDDDDD;
 margin-bottom: 1em;
 padding: 0.8em;
}
.error, .alert {
  background: none repeat scroll 0 0 #FBE3E4;
  border-color: #FBC2C4;
  color: #8A1F11;
  ul {
    margin:0;
    padding:0;
  }
}
.notice {
  background: none repeat scroll 0 0 #D5EDF8;
  border-color: #92CAE4;
  color: #205791;
}
.list {
  .container {
    border-bottom: 1px solid #DDDDDD;
    padding: 5px 10px; 
  }
}
.forgot {
  padding:10px;
  .unit {
    padding:10px 0;
  }
}
#user_remember_me {
  float:left;
  margin:4px;
}
.required {
  border:#f00;
}
